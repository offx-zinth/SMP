from math_utils import add, multiply


def compute_sum(x: int, y: int) -> int:
    """Computes sum using utils."""
    return add(x, y)


def compute_product(x: int, y: int) -> int:
    """Computes product using utils."""
    return multiply(x, y)
